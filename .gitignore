.DS_Store
.env
.env.*
!.env.example
.flaskenv
*.pyc
*.pyo
env/
venv/
.venv/
env*
dist/
build/
*.egg
*.egg-info/
.tox/
.cache/
.pytest_cache/
.hypothesis/
cover/

# Data directories
src/data/
data/
benchmarks/qa_runs/
benchmarks/kis_runs/

# Local runtime state
.runlogs/
.venv-broken-*/

.idea/
docs/_build/
.vscode

# Coverage reports
htmlcov/
.coverage
.coverage.*
*,cover

# Dict folders
src/dict/map-keyframes/
src/dict/media-info/
src/dict/objects/

# Runtime media-info archive for video playback / frame capture.
# Provide it on the deployment machine and point MEDIA_INFO_PATH at it; the
# extracted per-video JSON is large and machine-specific, never commit it.
media-info-aic25-b1.zip
media-info-*.zip

# BEiT3 / Faiss runtime artifacts (large, machine-specific; never commit)
*.pth
*.index
*.parquet
*.spm
*.npy

# Local Jina smoke-test index + its downloaded per-video embeddings/records
# (scratch/jina_smoke_test.py build-mini --out ./jina_mini). Azure-sourced,
# never commit.
/jina_mini/
/jina_runtime/
src/dict/nw/*.bin
src/dict/faiss_index.bin
.gstack/
src/dict/metadata_beit3.json
src/dict/*.json.bak_*
aic2026_05_beit3_faiss/faiss_index_downloaded.bin
aic2026_05_beit3_faiss/metadata_beit3_downloaded.json

# Terraform
*.tfvars
*.tfvars.json
*.tfstate
*.tfstate.*
.terraform/
.terraform.lock.hcl
crash.log

# Generated cloud-asset manifest (ops artifact, per dataset version)
hcmai-assets.json
