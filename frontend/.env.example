# demo: always use local mock results
# auto: use FastAPI when reachable, otherwise use local mock results
# live: require FastAPI and surface every API/configuration error
VITE_SEARCH_MODE=auto

# FastAPI origin. It may include /users, but it is not required.
# VITE_SEARCH_API_BASE_URL=http://127.0.0.1:3000
